# Uptime Monitor

A simple FastAPI application scaffold for monitoring service availability.

## Run locally

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Start the app:
   ```bash
   uvicorn app.main:app --reload
   ```

3. Open http://127.0.0.1:8000 in a browser.
